[project]
name = "counterpart"
version = "0.1.0"
description = "Test your A2A agent against simulated counterparties — cooperative, broken, or hostile — before you connect it to a real one."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "counterpart contributors" }]
license = "Apache-2.0"
license-files = ["LICENSE"]
keywords = ["a2a", "agent2agent", "testing", "mock", "adversarial", "agents", "pytest"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Framework :: Pytest",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Testing :: Mocking",
    "Typing :: Typed",
]
dependencies = [
    "pydantic>=2.7",
    "starlette>=0.37",
    "httpx>=0.27",
    "uvicorn>=0.30",
    "typer>=0.12",
    "rich>=13.7",
    "pyyaml>=6.0",
    "pytest-asyncio>=0.23",
]

[project.scripts]
counterpart = "counterpart.cli.main:app"

[project.entry-points.pytest11]
counterpart = "counterpart.pytest_plugin.plugin"

[dependency-groups]
dev = [
    "pytest>=8.2",
    "pytest-asyncio>=0.23",
    "ruff==0.15.22",  # pinned: CI and local must agree (a newer ruff formats md code blocks)
    "mypy>=1.10",
    "types-pyyaml>=6.0",
    "jsonschema>=4.22",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/counterpart"]

[tool.ruff]
line-length = 100
src = ["src", "tests"]
target-version = "py311"
# Never reformat Markdown: newer ruff formats fenced Python inside docs, which would rewrite
# the README's deliberately aligned explanatory comments.
extend-exclude = ["*.md"]

[tool.ruff.lint]
select = [
    "E",     # pycodestyle errors
    "W",     # pycodestyle warnings
    "F",     # pyflakes
    "I",     # isort
    "UP",    # pyupgrade
    "B",     # bugbear
    "SIM",   # simplify
    "C4",    # comprehensions
    "RUF",   # ruff-specific
    "ASYNC", # async pitfalls (blocking calls in async code)
    "T20",   # no stray print (CLI modules opt out)
]

[tool.ruff.lint.per-file-ignores]
"src/counterpart/cli/**" = ["T20"]
"tests/**" = ["T20"]
"examples/**" = ["T20"]  # runnable demos print their results by design

[tool.mypy]
python_version = "3.11"
strict = true
mypy_path = "src"
packages = ["counterpart"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
